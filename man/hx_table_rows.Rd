% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hx_table.R
\name{hx_table_rows}
\alias{hx_table_rows}
\title{Table rows fragment}
\usage{
hx_table_rows(data, columns = NULL, col_classes = NULL)
}
\arguments{
\item{data}{A data frame.}

\item{columns}{Character vector of column names to include (and their order).
If \code{NULL}, all columns are used.}

\item{col_classes}{Named list of CSS classes to add to \verb{<td>} elements,
keyed by column name. Example: \code{list(price = "text-end fw-bold")}.}
}
\value{
A \link[htmltools:tagList]{htmltools::tagList} of \verb{<tr>} tags.
}
\description{
Converts a data frame into a \code{tagList} of \verb{<tr>} elements, one per row.
Designed to be used as a fragment endpoint response — the output replaces
a \verb{<tbody>} via htmx swap.
}
\section{Serving the fragment}{

Annotate the route with \verb{@serializer htmx}. plumber2's own \code{html}
serializer does not understand a \code{tagList} and would send unusable markup.
See \link{htmx-serializer}.
}

\examples{
df <- data.frame(cut = c("Fair", "Good"), price = c(326L, 400L))
hx_table_rows(df, columns = c("cut", "price"))

# With CSS classes on specific columns
hx_table_rows(df, col_classes = list(price = "text-end fw-bold"))

}
\seealso{
\link{htmx-serializer}
}
